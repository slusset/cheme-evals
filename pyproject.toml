[project]
name = "cheme-evals"
version = "0.1.0"
description = "ChemE domain eval harness for learning eval skills and best practices"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
